mixin checkbox(value, name, checked = false)
    .check.checkbox(class!=attributes.class)
        input(type="checkbox" name=name value=value checked=checked ? 'checked' : null)
        .checkbox__box
            +img('check.svg', 'icon')
        .checkbox__text= value